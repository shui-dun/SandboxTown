package com.shuidun.sandbox_town_backend.observer;

public class MapObserver extends AbstractObserver {

    public MapObserver() {
        // mp.put(EventEnum.FOO, foo);
        // ObserverNotifier.register(EventEnum.FOO, this);
    }
}
